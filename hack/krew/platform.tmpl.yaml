  - selector:
      matchLabels:
        os: ${OS}
        arch: ${ARCH}
    uri: https://github.com/${REPO}/releases/download/${TAG}/${ASSET}
    sha256: ${SHA256}
    files:
      - from: kubectl-waitx
        to: .
    bin: kubectl-waitx
